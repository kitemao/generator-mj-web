language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mj-web' ]; then cd .. && eval "mv $currentfolder generator-mj-web" && cd generator-mj-web; fi

